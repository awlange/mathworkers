/* mathworkers main */

// Base function.
var mathworkers = function() {
  // Add functionality here.
  return true;
};


// Version.
mathworkers.VERSION = '0.0.0';


// Export to the root, which is probably `window`.
root.mathworkers = mathworkers;
